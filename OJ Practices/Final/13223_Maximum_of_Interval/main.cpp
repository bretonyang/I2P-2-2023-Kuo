#include <iostream>
#include <deque>

using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int N, L;
    cin >> N >> L;

    deque<int> dq;
    for (int i = 0; i < N; i++) {
        int num;
        cin >> num;
    }

    cout << endl;

    return 0;
}
